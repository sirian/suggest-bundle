sirian_suggest:
    path:  /{name}/suggest
    defaults: { _controller: SirianSuggestBundle:Suggest:suggest }

sirian_suggest_init:
    path:  /{name}/init
    defaults: { _controller: SirianSuggestBundle:Suggest:init }
